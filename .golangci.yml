run:
  timeout: 5m
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - revive
    - misspell
    - gosec
    - unconvert
    - gocritic
issues:
  exclude-rules:
    - path: _test\.go
      linters: [gosec]
