{
  "name": "@korneza/outpost-client",
  "version": "0.1.0",
  "description": "TypeScript client for MCP agents behind Outpost, with automatic direct-connection fallback if the proxy is unreachable.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "!dist/**/*.test.*"],
  "license": "Apache-2.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  }
}
